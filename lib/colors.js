// Preset colors a class can be tagged with. Stored on each class as one of
// these keys (e.g. "blue") - the actual light/dark hex values live in
// globals.css as `.class-color-*` utility classes, so components never
// touch a hex code directly.
export const CLASS_COLORS = [
  "blue",
  "teal",
  "green",
  "amber",
  "orange",
  "red",
  "pink",
  "purple",
];

export const DEFAULT_CLASS_COLOR = "blue";
